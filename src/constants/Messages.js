const messages = Object.freeze({
    NOTIFY_MISSING_GEO_LOCATION_PERMISSIONS: 'Please enable geo location permissions in the Amazon Alexa app.',
    NOTIFY_MISSING_DEVICE_ADDRESS_PERMISSIONS: 'Please enable device address permissions in the Amazon Alexa app.',
});

module.exports = {
    messages: messages
};
